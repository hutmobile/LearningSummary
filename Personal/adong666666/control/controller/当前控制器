        if presentingViewController != shouyeViewController{
            self.player?.releasePlayer()
        }
        
        
        
        
        咋一看，还真注意不到它存在问题
        
        加个括号就好了
        
       if presentingViewController != shouyeViewController(){
            self.player?.releasePlayer()
        }
        
